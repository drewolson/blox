<%= render Blox.SharedView, "_errors.html", Dict.merge(assigns, errors: @changeset.errors) %>

<%= form_for @changeset, post_path(@conn, :create), fn f -> %>
  <%= render Blox.PostView, "_form.html", Dict.merge(assigns, f: f) %>

  <%= submit "Create" %>
<% end %>
